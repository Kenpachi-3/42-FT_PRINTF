# 42-FT_PRINTF
Recreating printf() from libc as part of the 42 Common Core. Implements variadic functions in C to handle conversions for characters, strings, integers, unsigned integers, hexadecimal and pointers.
